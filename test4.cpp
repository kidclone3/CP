#include<bits/stdc++.h>
using namespace std;
int main() {
    // stack<pair<char, int>> st;
    // string s; cin >> s;
    // for(int i = 0; i < s.size(); ++i) {
    //     if(i % 2 == 0) st.push({s[i], i});
    //     // cout << st.top(). << " ";
    //     if(i%3==0) st.pop();
    // }
    // cout << "\n";
    // // cout << st.size() << '\n';
    // while(!st.empty()) {
    //     cout << st.top().first << " " << st.top().second << "\n";
    //     st.pop();
    // }
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    cout << "cout ";
    cerr <<"cerr ";
    printf("printf ");
}

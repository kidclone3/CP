#include <bits/stdc++.h>
using namespace std;

int openLock(vector<string>& deadends, string target) {
    map<string, bool> check;
    for(auto it : deadends) {
        check[it] = 1;
    }
}
int bfs(map<string, bool> &check, string target) {
    string init = "0000";
    queue<pair<int, int>> q;
    q.push({0, 0});
    while(!q.empty()) {
        string tmp = init;
    }

}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
}

